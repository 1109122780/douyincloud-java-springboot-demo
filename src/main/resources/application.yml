server:
  port: 8000

spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_ADDRESS}
      database: demo
      authentication: admin

  redis:
    url: redis://${REDIS_USERNAME}:${REDIS_PASSWORD}@${redis-cnlf6jk9ck61m53ez.redis.ivolces.com:6379}
    timeout: 1000

cloud:
  env: ${CLOUD_ENV}

